@model IEnumerable<AppData.model.Anh>

@{
    ViewData["Title"] = "GellAllAnh";
}

<h1>GellAllAnh</h1>

<p>
    <a asp-action="AddAnh">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Connect)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                    <img src="@Url.Action("DisplayImage", "QuanTri", new { imagePath = item.Connect })" alt="Image" width="30%" height="30%" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
               @* @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
